diff --git a/central_server/.env.sample b/central_server/.env.sample
new file mode 100644
index 0000000..d00491e
--- /dev/null
+++ b/central_server/.env.sample
@@ -0,0 +1,4 @@
+# Central server environment variables
+SECRET_KEY=supersecretkey
+ACCESS_TOKEN_EXPIRE_MINUTES=30
+DATABASE_URL=sqlite:///./central_server.db
diff --git a/central_server/main.py b/central_server/main.py
new file mode 100644
index 0000000..5ec98f1
--- /dev/null
+++ b/central_server/main.py
@@ -0,0 +1,27 @@
+from fastapi import FastAPI
+from fastapi.middleware.cors import CORSMiddleware
+
+app = FastAPI(title="LoopBreak Central Server")
+
+# Allow CORS for local dev
+app.add_middleware(
+    CORSMiddleware,
+    allow_origins=["*"],
+    allow_credentials=True,
+    allow_methods=["*"],
+    allow_headers=["*"],
+)
+
+@app.get("/")
+def root():
+    return {"message": "LoopBreak Central Server is running"}
+
+@app.get("/api/health")
+def health_check():
+    return {"status": "ok"}
+
+# TODO:
+# - Add authentication (JWT)
+# - Connect to DB
+# - Add endpoints for Employee & HR frontends
+# - Integrate backend processing functions
diff --git a/central_server/requirements.txt b/central_server/requirements.txt
new file mode 100644
index 0000000..4976df1
--- /dev/null
+++ b/central_server/requirements.txt
@@ -0,0 +1,3 @@
+fastapi
+uvicorn
+python-dotenv
